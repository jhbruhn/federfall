// Result now lives in zugvogel_core (eiermann-d2a.3), unchanged apart from
// using meta's @immutable instead of flutter/foundation's, so the package pulls
// in no Flutter.
export 'package:zugvogel_core/zugvogel_core.dart' show Err, Ok, Result;
