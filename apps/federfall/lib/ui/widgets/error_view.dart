// ErrorView now lives in zugvogel_ui (eiermann-d2a.9). Its title and retry
// label arrive through the injected ZugvogelStrings; the shape of a failed
// surface — icon, message, one retry affordance — is shared.
export 'package:zugvogel_ui/zugvogel_ui.dart' show ErrorView;
