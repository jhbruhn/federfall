// EmptyView now lives in zugvogel_ui (eiermann-d2a.9). Its one piece of copy —
// the generic "nothing here yet" fallback — became an injected string, so the
// widget no longer knows which product it is standing in.
export 'package:zugvogel_ui/zugvogel_ui.dart' show EmptyView;
