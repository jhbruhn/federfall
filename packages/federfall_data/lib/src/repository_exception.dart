// RepositoryException now lives in zugvogel_data (eiermann-d2a.2), with the
// same status-code classification and the same unknownOutcome kind for a write
// whose outcome cannot be known.
export 'package:zugvogel_data/zugvogel_data.dart'
    show RepositoryErrorKind, RepositoryException;
